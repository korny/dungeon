describe("TeapotController", function() {
  var controller;

  beforeEach(function() {
    controller = new TeapotController();
  });

  it("does something", function() {
    expect(1).toEqual(1);
  });
});
